apiVersion: v1
kind: Service
metadata:
  name: {{ include "nodeappjs.fullname" . }}
  labels:
    {{- include "nodeappjs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nodeappjs.selectorLabels" . | nindent 4 }}
